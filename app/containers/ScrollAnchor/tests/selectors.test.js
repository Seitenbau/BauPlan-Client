// import { fromJS } from 'immutable';
// import { makeSelectScrollAnchorDomain } from '../selectors';

// const selector = makeSelectScrollAnchorDomain();

describe('makeSelectScrollAnchorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
