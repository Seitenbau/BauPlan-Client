// import {
//   makeSelectScale,
// } from 'containers/Floor/selectors';
// import { fromJS } from 'immutable';
// import { createSelector } from 'reselect';

describe('Floors', () => {
  describe('makeSelectScale', () => {
    it('schould select scale from state', () => {
      // const state = fromJS({
      //   img: {
      //     scale: 0.34,
      //   },
      // });
      // const sut = makeSelectScale(state);
      // expect(sut()).toEqual(0.34);
    });
  });
});
