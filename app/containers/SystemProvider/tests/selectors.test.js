// import { fromJS } from 'immutable';
// import { makeSelectSystemProviderDomain } from '../selectors';

// const selector = makeSelectSystemProviderDomain();

describe('makeSelectSystemProviderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
