// import { fromJS } from 'immutable';
// import { makeSelectScrollListenerDomain } from '../selectors';

// const selector = makeSelectScrollListenerDomain();

describe('makeSelectScrollListenerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
