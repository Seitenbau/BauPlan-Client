// import { fromJS } from 'immutable';
// import { makeSelectJumpViewDomain } from '../selectors';

// const selector = makeSelectJumpViewDomain();

describe('makeSelectJumpViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
