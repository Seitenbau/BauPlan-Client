// import { fromJS } from 'immutable';
// import { makeSelectTableParentDomain } from '../selectors';

// const selector = makeSelectTableParentDomain();

describe('makeSelectTableParentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
