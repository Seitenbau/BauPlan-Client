// import { fromJS } from 'immutable';
// import { makeSelectObjectListDomain } from '../selectors';

// const selector = makeSelectObjectListDomain();

describe('makeSelectObjectListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
