// import { fromJS } from 'immutable';
// import { makeSelectSearchFieldDomain } from '../selectors';

// const selector = makeSelectSearchFieldDomain();

describe('makeSelectSearchFieldDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
